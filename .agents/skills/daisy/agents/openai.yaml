interface:
  display_name: "Daisy"
  short_description: "Repo-specific patterns and workflows for daisy"
  default_prompt: "Use the daisy repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true